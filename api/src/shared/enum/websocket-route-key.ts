export enum WebSocketRouteKey {
  CONNECT = '$connect',
  DISCONNECT = '$disconnect',
  DEFAULT = '$default',
  SEND_MESSAGE = 'sendMessage',
  SYSTEM_MESSAGE = 'systemMessage',
  RECEIVE_MESSAGE = 'receiveMessage'
}
